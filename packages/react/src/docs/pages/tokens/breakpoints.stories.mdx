import { Meta } from "@storybook/addon-docs";

import { BreakpointsGrid } from "../../components/breakpoints-grid";

<Meta title="Tokens/Breakpoints" />

# Breakpoints

Breakpoints tokens define the widths at which a website's content will respond to provide the user with the best layout to consume the information. Using named breakpoints instead of hardcoded pixel values promotes consistency and eases maintenance.

## Table of Contents

-   [Breakpoint Values](#breakpoint-values)

## Breakpoint Values

Here are the standard breakpoints used across the application to ensure a responsive and consistent layout on different devices.

<BreakpointsGrid />
